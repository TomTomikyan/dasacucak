# College Schedule Generator - FastAPI Backend

Полноценный бэкенд для генератора расписания колледжа, написанный на Python FastAPI.

## Особенности

- 🚀 **FastAPI** - современный, быстрый веб-фреймворк
- 🗄️ **SQLAlchemy** - мощная ORM для работы с базой данных
- 📊 **Pydantic** - валидация данных и сериализация
- 🔄 **Автоматическая документация API** - Swagger UI и ReDoc
- 🎯 **Умная генерация расписания** - полный перенос логики из React
- 📁 **Импорт/Экспорт конфигурации** - сохранение и загрузка данных
- 🏫 **Специализированные аудитории** - поддержка лабораторий и кабинетов преподавателей

## Установка и запуск

### 1. Установка зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и настройте переменные:

```bash
cp .env.example .env
```

### 3. Запуск сервера

```bash
python run.py
```

Или через uvicorn:

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## API Документация

После запуска сервера документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## Основные эндпоинты

### Учреждение
- `GET /api/institution` - получить настройки учреждения
- `POST /api/institution` - создать учреждение
- `PUT /api/institution` - обновить настройки

### Группы
- `GET /api/class-groups` - получить все группы
- `POST /api/class-groups` - создать группу
- `PUT /api/class-groups/{id}` - обновить группу
- `DELETE /api/class-groups/{id}` - удалить группу
- `POST /api/class-groups/bulk-generate` - массовое создание групп

### Предметы
- `GET /api/subjects` - получить все предметы
- `POST /api/subjects` - создать предмет
- `PUT /api/subjects/{id}` - обновить предмет
- `DELETE /api/subjects/{id}` - удалить предмет

### Аудитории
- `GET /api/classrooms` - получить все аудитории
- `POST /api/classrooms` - создать аудиторию
- `PUT /api/classrooms/{id}` - обновить аудиторию
- `DELETE /api/classrooms/{id}` - удалить аудиторию
- `POST /api/classrooms/bulk-generate` - массовое создание аудиторий

### Преподаватели
- `GET /api/teachers` - получить всех преподавателей
- `POST /api/teachers` - создать преподавателя
- `PUT /api/teachers/{id}` - обновить преподавателя
- `DELETE /api/teachers/{id}` - удалить преподавателя

### Расписание
- `GET /api/schedule` - получить расписание
- `POST /api/schedule/generate` - сгенерировать расписание
- `PUT /api/schedule/{id}` - обновить слот расписания
- `DELETE /api/schedule/{id}` - удалить слот
- `DELETE /api/schedule` - очистить всё расписание

### Конфигурация
- `GET /api/configuration/export` - экспорт конфигурации
- `POST /api/configuration/import` - импорт конфигурации
- `DELETE /api/configuration/clear` - очистить все данные

## База данных

По умолчанию используется SQLite для простоты разработки. Для продакшена рекомендуется PostgreSQL.

### Миграции

Структура базы данных создается автоматически при первом запуске.

## Логика генерации расписания

Алгоритм генерации расписания включает:

1. **Валидация данных** - проверка корректности входных данных
2. **Приоритизация** - сортировка занятий по важности
3. **Умное распределение** - учет специализированных аудиторий
4. **Оптимизация** - балансировка нагрузки по дням
5. **Логирование** - подробные логи процесса генерации

### Особенности:

- 🏫 **Специализированные лаборатории** - автоматическое назначение предметов в соответствующие аудитории
- 👨‍🏫 **Кабинеты преподавателей** - приоритет использования собственных кабинетов
- ⚖️ **Балансировка нагрузки** - равномерное распределение занятий по дням
- 🔄 **Автоназначение преподавателей** - автоматическая привязка преподавателей к предметам

## Структура проекта

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py              # Основное приложение FastAPI
│   ├── models.py            # SQLAlchemy модели
│   ├── schemas.py           # Pydantic схемы
│   ├── crud.py              # CRUD операции
│   ├── database.py          # Настройка базы данных
│   └── schedule_generator.py # Логика генерации расписания
├── requirements.txt         # Зависимости Python
├── .env.example            # Пример переменных окружения
├── run.py                  # Скрипт запуска
└── README.md               # Документация
```

## Интеграция с React

Для интеграции с существующим React приложением:

1. Обновите все API вызовы для работы с FastAPI эндпоинтами
2. Замените localStorage на API вызовы
3. Обновите логику генерации расписания для использования бэкенда

Пример интеграции:

```typescript
// Вместо localStorage
const response = await fetch('http://localhost:8000/api/class-groups');
const classGroups = await response.json();

// Генерация расписания
const response = await fetch('http://localhost:8000/api/schedule/generate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ clear_existing: true })
});
const result = await response.json();
```

## Производительность

- Асинхронная обработка запросов
- Оптимизированные SQL запросы
- Кэширование результатов (можно добавить Redis)
- Пагинация для больших наборов данных

## Безопасность

- Валидация всех входных данных через Pydantic
- CORS настройки для безопасного взаимодействия с фронтендом
- Возможность добавления аутентификации и авторизации

## Развертывание

Для продакшена рекомендуется:

1. Использовать PostgreSQL вместо SQLite
2. Настроить переменные окружения
3. Использовать Docker для контейнеризации
4. Настроить reverse proxy (nginx)
5. Добавить мониторинг и логирование